{
  "name": "prince-of-wales-iffley-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/check-html.mjs",
    "preview": "vite preview",
    "images": "node scripts/images.mjs",
    "check": "node scripts/check-html.mjs"
  },
  "devDependencies": {
    "sharp": "^0.34.3",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
